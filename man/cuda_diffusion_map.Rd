% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{cuda_diffusion_map}
\alias{cuda_diffusion_map}
\title{Diffusion-map-style embedding}
\usage{
cuda_diffusion_map(
  x,
  n_components = 2L,
  sigma = NULL,
  diffusion_time = 1,
  metric = c("euclidean", "cosine"),
  device = c("auto", "cuda", "cpu"),
  reduced_dim = NULL
)
}
\arguments{
\item{x}{Numeric observation-by-feature matrix, compatible cudaverse result,
or a \code{SingleCellExperiment} with a reduced dimension.}

\item{n_components}{Output dimensions.}

\item{sigma}{Gaussian kernel bandwidth. Defaults to the median positive
pairwise distance.}

\item{diffusion_time}{Non-negative diffusion time exponent.}

\item{metric}{Euclidean or cosine distance.}

\item{device}{Device passed to \code{\link[=cuda_distance]{cuda_distance()}}.}

\item{reduced_dim}{For a \code{SingleCellExperiment}, the reduced-dimension name
to embed. See \code{\link[=cuda_umap]{cuda_umap()}} for automatic selection.}
}
\value{
A \code{cuda_embedding} with the stable fields documented by
\code{\link[=cuda_umap]{cuda_umap()}}, stage-level distance/kernel/eigendecomposition provenance,
an optional \code{distance_input} stage when resident native storage is reused,
and an additional \code{eigenvalues} element.
}
\description{
Pairwise distances can use the cudaverse CUDA path. Kernel construction
and eigendecomposition currently run on the CPU.
}
\examples{
cuda_diffusion_map(
  matrix(rnorm(120), 40, 3),
  n_components = 2,
  device = "cpu"
)
}
