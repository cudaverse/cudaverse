% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse.R
\name{cuda_sparse}
\alias{cuda_sparse}
\title{Create a GPU-aware sparse matrix}
\usage{
cuda_sparse(
  x,
  format = c("csr", "coo"),
  device = c("auto", "cuda", "cpu"),
  drop_zeros = TRUE
)
}
\arguments{
\item{x}{A numeric matrix, a sparse matrix from the \code{Matrix} package, or a
\code{cudasparse} object.}

\item{format}{Logical storage format, \code{"csr"} or \code{"coo"}.}

\item{device}{One of \code{"auto"}, \code{"cuda"}, or \code{"cpu"}.}

\item{drop_zeros}{Whether to remove explicitly stored zeros.}
}
\value{
A \code{cudasparse} list. Stable public metadata include one-based COO
\code{i} and \code{j}, numeric \code{values}, zero-based CSR \code{row_ptr} and \code{col_index},
integer \code{shape}, matrix \code{dimnames}, logical \code{format}, actual \code{device}, and
\code{backend}. \code{storage} is backend-internal and should not be accessed
directly.
}
\description{
Create a GPU-aware sparse matrix
}
\details{
Existing \code{cudasparse} inputs use their stable sorted COO mirror
directly. Same-device format changes share backend storage; transfers and
zero filtering do not construct an intermediate Matrix object.
}
\examples{
library(Matrix)
x <- rsparsematrix(5, 4, density = 0.25)
cuda_sparse(x, device = "cpu")
}
