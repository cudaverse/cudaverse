% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse.R
\name{sparse_normalize}
\alias{sparse_normalize}
\title{Normalize sparse rows or columns without densifying}
\usage{
sparse_normalize(
  x,
  margin = c("rows", "columns"),
  scale_factor = 1,
  log1p = FALSE
)
}
\arguments{
\item{x}{A non-negative \code{cudasparse} matrix.}

\item{margin}{Normalize \code{"rows"} or \code{"columns"}.}

\item{scale_factor}{Positive target sum before the optional log transform.}

\item{log1p}{Whether to apply \code{log1p()} to normalized stored values.}
}
\value{
A \code{cudasparse} matrix on the same device as \code{x}.
}
\description{
Each selected row or column is divided by its sum and multiplied by
\code{scale_factor}. Optionally, \code{log1p()} is applied to stored non-zero values.
The operation preserves sparse structure and dimension labels.
The native CUDA backend retains normalized storage on the device and updates
the public host COO mirror from metadata already held by the object. It does
not download the normalized values or the complete margin-sum vector; only
a small device-validation flag crosses back before the result is returned.
}
\examples{
x <- cuda_sparse(matrix(c(1, 0, 3, 2), 2), device = "cpu")
sparse_normalize(x, margin = "rows", scale_factor = 1)
}
