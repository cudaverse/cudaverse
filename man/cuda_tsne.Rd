% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{cuda_tsne}
\alias{cuda_tsne}
\title{t-SNE embedding}
\usage{
cuda_tsne(
  x,
  n_components = 2L,
  perplexity = 30,
  theta = 0.5,
  seed = NULL,
  ...,
  reduced_dim = NULL
)
}
\arguments{
\item{x}{Numeric observation-by-feature matrix, compatible cudaverse result,
or a \code{SingleCellExperiment} with a reduced dimension.}

\item{n_components}{Output dimensions.}

\item{perplexity}{t-SNE perplexity.}

\item{theta}{Barnes-Hut accuracy/speed trade-off.}

\item{seed}{Optional random seed.}

\item{...}{Additional arguments passed to \code{Rtsne::Rtsne()}.}

\item{reduced_dim}{For a \code{SingleCellExperiment}, the reduced-dimension name
to embed. When \code{NULL}, a compatible recorded metadata choice is used
first, followed by a uniquely named \code{"PCA"}. Other names must be selected
explicitly.}
}
\value{
A \code{cuda_embedding}; see \code{\link[=cuda_umap]{cuda_umap()}} for the stable result fields.
}
\description{
t-SNE currently uses the CPU \code{Rtsne} backend.
}
\examples{
if (requireNamespace("Rtsne", quietly = TRUE)) {
  cuda_tsne(matrix(rnorm(120), 40, 3), perplexity = 5, seed = 1)
}
}
