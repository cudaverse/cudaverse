% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddings.R
\name{cuda_umap}
\alias{cuda_umap}
\title{UMAP embedding}
\usage{
cuda_umap(
  x,
  n_components = 2L,
  n_neighbors = 15L,
  min_dist = 0.1,
  metric = "euclidean",
  n_epochs = NULL,
  seed = NULL,
  ...,
  reduced_dim = NULL
)
}
\arguments{
\item{x}{Numeric observation-by-feature matrix, compatible cudaverse result,
or a \code{SingleCellExperiment} with a reduced dimension.}

\item{n_components}{Output dimensions.}

\item{n_neighbors}{Number of nearest neighbours.}

\item{min_dist}{Minimum UMAP distance.}

\item{metric}{Distance metric passed to \code{uwot::umap()}.}

\item{n_epochs}{Optional training epochs.}

\item{seed}{Optional random seed.}

\item{...}{Additional arguments passed to \code{uwot::umap()}.}

\item{reduced_dim}{For a \code{SingleCellExperiment}, the reduced-dimension name
to embed. When \code{NULL}, a compatible recorded metadata choice is used
first, followed by a uniquely named \code{"PCA"}. Other names must be selected
explicitly.}
}
\value{
A \code{cuda_embedding} list containing \code{coordinates}, \code{method},
\code{backend}, \code{compute_device}, per-stage \code{compute_stages}, source metadata,
and algorithm \code{parameters}.
}
\description{
UMAP currently uses the CPU \code{uwot} backend. GPU-aware cudaverse inputs are
accepted and their source device is retained in the result metadata.
}
\examples{
if (requireNamespace("uwot", quietly = TRUE)) {
  cuda_umap(matrix(rnorm(120), 40, 3), n_neighbors = 5, seed = 1)
}
}
